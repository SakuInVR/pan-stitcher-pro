# Pan Stitcher Pro

上下または左右にパンする動画を、ブラウザ内で1枚の画像へ結合するツールです。動画は外部へ送信されません。

## 公開版

GitHub Pagesへ自動デプロイされます。`main` ブランチへのpushで公開版が更新されます。

```powershell
npm install
npm run dev
```

表示されたローカルURLをブラウザで開き、動画・方向・対象時間を指定して生成します。
